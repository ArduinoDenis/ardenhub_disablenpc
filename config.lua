Config = {}

Config.DisableNPC = {
    peds = true,              -- Disable pedestrians
    vehicles = true,          -- Disable NPC vehicles
    emergencyVehicles = true, -- Disable emergency vehicles
    garbageTrucks = true,     -- Disable garbage truck
    boats = true,             -- Disable NPC boats
    aircraft = true           -- Disable NPC aircraft/helicopters
}

Config.DensityMultiplier = {
    pedDensity = 0.0,     -- Pedestrian density multiplier (0.0 = none)
    vehicleDensity = 0.0  -- Vehicle density multiplier (0.0 = none)
}